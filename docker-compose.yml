version: '3'

services:
  web:
    build: .
    ports:
    - 8080:8080
    env_file:
    - .env



  mongodb:
    image: mongo:4.2.3-bionic
    container_name: mongodb
    ports:
    - 27017:27017


volumes:
  data:
